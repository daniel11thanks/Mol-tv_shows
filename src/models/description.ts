export type DescriptionList = {
    score: string;
    show: {
        averageRuntime: number;
        name: string;
        image: { medium: string, original: string };
        id: number;
        premiered: number;
        status: string;
        summary: string;
    };
    
};
